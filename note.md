# Stage 1 — Explore the Project

## Task 01 — Project Hunt
###  知识点
- `pwd`：打印当前工作目录（绝对路径）
- `ls -a`：列出全部文件，**能看到 . 开头的隐藏文件**
- `cd ..`：返回上一级目录
- `cat`：查看文件内容，`cat check.sh` 是排错神器
- 相对路径：从当前目录出发，用 `.` `..` 跳转
- 隐藏文件：文件名以 `.` 开头（例：`.project`）

###  踩坑心得
1. `check.sh` 校验 **WSL本地仓库**，GitHub网页修改远端文件，本地不会同步
2. 一个任务可能有多个条件，全部满足才 PASS（Stage01 需要两个txt文件都正确）
3. 遇到 FAIL，优先 `cat check.sh`，看脚本要求，不要盲猜答案
## Task 02 — Missing Command
###  知识点
- `ls -l`：长格式列出文件，查看文件权限、所有者信息
- `chmod`：修改文件权限；`chmod +x` 添加可执行权限
- 执行权限：文件要有 x 权限，才可以作为程序运行
- `./`：代表当前目录，`./文件名` 表示运行当前目录下的程序
- `PATH`：环境变量，保存系统查找命令的目录列表
- `command -v`：检查命令是否存在，能找到则输出命令路径
- `which`：查找可执行命令所在的完整路径
- `export`：导出环境变量；`export PATH=$PATH:./tools` 临时追加目录到PATH，仅当前终端会话生效

###  踩坑心得
1. export 修改 PATH 只在当前终端生效，新开终端失效（题目要求，不能改 .bashrc 永久生效）
2. 执行校验 `./check.sh 03`，必须和 export 在同一个终端窗口，换终端就失败
3. 必须先 cd 进入仓库根目录，`./tools` 这个相对路径才正确
# Stage 2 — Search the Project
## Task3 — Code Search
###  知识点
- `find`：递归遍历目录，`-type f` 筛选普通文件（f=普通文件，d=目录）
- `grep`：文本检索；`-l` 只输出匹配成功的文件名；`-E` 启用扩展正则，`|` 表示或
- `sort`：默认按字典序升序排序
- `uniq`：去除重复行，**只能删除相邻重复，必须先 sort 再 uniq**
- `-exec ... \;`：find 的动作参数，对找到的文件执行后续命令
###  踩坑心得
- 误将 `-type f` 写到 grep 参数后，报错 `unrecognized option '--type'`，记住 `-type` 是 find 的参数，不属于 grep
- 正则 `TODO|FIXME` 如果不加引号，shell 会把 `|` 当成管道符，扩展正则一定要加引号
- WSL 第一次执行 `git commit`，出现 `Author identity unknown`，commit 前需要用 `git config` 配置 user.name 和 user.email
## Task 04 — Log Statistics
### 知识点
1. 管道符 `|`：前一条命令输出作为后一条命令输入，串联多条命令处理文本
2. `grep`：文本筛选，`grep -c` 统计匹配行数
3. `awk`：按列提取字段，`$1 $2 $3` 代表不同列，`-F` 修改分隔符
4. `sort`：文本排序，`-n` 按数字排序，`-nr` 数字降序
5. `uniq`：去重，`uniq -c` 统计重复出现次数
6. `head`：截取文件前N行，获取高频条目

### 踩坑心得
1. sort不加-n会按字符排序，数字10会排在2前面，频次统计要加-n
2. 写awk前先head查看日志，确认字段位置，避免提取错列
3. 管道命令顺序不能颠倒：grep筛选 → awk提取 → sort排序 → uniq计数 → sort降序 → head取top
# Stage 3 — Connect the Tools
## Task 05 — Pipeline Challenge
### 知识点
1. `cut`：按分隔符截取字段，-d 指定分隔符，-f 指定取第几列
2. 管道符` |`：多命令直接串联，全程不生成中间临时文件
3. `sort`：排序，`uniq` 前置必须排序
4. `uniq -c`：统计条目出现次数
5. `sort -nr`：按数字大小降序排列
6. `head -n1`：只取出排序后的第一条结果

### 踩坑心得
1. `cut` 使用空格作为分隔符时，遇到连续多个空格容易切错字段，`awk `自动忽略连续空格，稳定性更好
2.`uniq -c` 的输出前面带有计数字段，需要再次提取，只保留IP写入结果文件
3. 题目硬性要求：禁止创建中间临时文件，必须全部依靠管道传递数据
4. `sort` 一定要加 `-nr`，不加会按字符排序，高频结果排序错误
## Task 06 — Streams & Redirection
### 知识点
1. `stdin`：标准输入（文件描述符0），程序读取外部输入，用 `<` 重定向，本任务未使用
2. `stdout`：标准正常输出（文件描述符1），用 `>` 重定向保存到文件
3. `stderr`：标准错误输出（文件描述符2），用 `2>` 重定向保存到文件
4. `/dev/null`：空设备文件，用来丢弃不需要的输出
5. `tee`：读取输入，一边打印终端，一边写入文件

### 踩坑心得
1. `>` 只重定向正常输出，错误输出仍然会打印在终端，错误输出必须用 `2>` 单独处理
2. `tee` 默认只接收 stdout，如果不把 stderr 丢弃，错误信息也会写入 tee 目标文件
3. `>` 和 `2>` 不能写反，否则正常输出文件和错误输出文件内容颠倒
# Stage 4 — Automate the Work
## Task 07 — Analyze Script
### 知识点
1. `#!/usr/bin/env bash`：脚本shebang，必须放在脚本第一行，指定使用bash解释器执行脚本
2. `$#`：获取传入命令行参数的总个数
3. `$1`：获取第1个命令行参数
4. `[ ]`：bash条件判断，`-f` 判断文件是否存在
5. `exit 0`：正常退出，返回成功状态；`exit 1`：异常退出，返回非零状态
6. `$( )`：命令替换，把命令执行结果赋值给变量
7. `chmod +x`：给脚本添加可执行权限
8. `cut -d'=' -f2`：`cut` 按指定分隔符切割文本，`-d` 指定分隔符为`=`，`-f2`取分割后的第2段内容
9. `uniq -c`：统计重复行出现次数；`sort -nr`：按数字降序排序

### 踩坑心得
1. 脚本写完必须执行 `chmod +x` 添加可执行权限，否则无法直接运行
2. 判断参数数量用 `$#`，不能只用 `$1` 是否为空判断，容易出错
3. 文件路径变量建议加上双引号 `"$LOG_FILE"`，防止文件名带空格出错
4. 不同场景退出码要区分：参数错误、文件不存在返回非0；正常分析返回0
5. 日志字段为 `code=500` 时，不能直接输出整段，需要使用 `cut` 提取等号后面的纯数字，否则校验失败
6. VSCode编辑WSL内shell脚本，注意换行符要使用 `LF`，`CRLF` 会导致bash执行报错
## Task 08 — Script Debug
### 知识点
1. `shift`：把shell命令行参数列表向左移动一位，丢弃当前第一个参数
2. `$@`：代表全部剩余命令行参数；`"$@"`：带双引号，保留每个参数内部的空格（本题核心）
3. `for file in "$@"`：遍历所有传入文件参数
4. `cp "$file" "$DEST/"`：`cp` 复制文件，变量全部加双引号防止空格拆分

### 踩坑心得
1. 不带引号的 `$@` 会把带空格的文件名拆成多个参数，复制失败，必须写成 `"$@"`
2. `$var` 不加引号会触发单词拆分，变量内含空格时会出错；`"$var"` 抑制单词拆分
3. 脚本内所有文件路径变量推荐统一加上双引号，是shell脚本的好习惯
## Task 09 — Process Hunter
### 知识点
1. `pgrep -af worker-`：`-f` 匹配完整命令行文本，`-a` 输出完整命令；不加 `-f` 时pgrep只匹配可执行文件名，会匹配不到本题worker进程
2. `pgrep 进程名`：根据进程名字查找对应的进程PID
3. `PID`：进程编号，系统里每一个运行的进程都有唯一PID
4. `kill PID`：默认发送 `SIGTERM(15)` 信号，优雅正常终止进程
5. `signal`：Linux信号，不同数字代表不同进程控制信号

### 踩坑心得
1. 本题worker是 `sh -c while true; do sleep 60; done` 形式启动，不加 `-f` 的pgrep无法匹配，必须用 `pgrep -af`
2. 不能直接批量kill所有worker，必须精准找到 worker-beta 的PID单独终止
3. 优先使用默认 `kill`（SIGTERM）优雅结束进程，尽量避免 `kill -9`
# 截图
![alt text](image.png)
