# Task 08 Answer

请在这里简短回答：为什么 Shell 中 `$var` 和 `"$var"` 有时会产生不同的结果？

当变量的值里面含有空格、制表符等空白字符时，`$var` 会发生单词拆分（word splitting），Shell会把它拆成多个独立参数；而 `"$var"` 加上双引号后，会抑制单词拆分，整个变量的值会作为**一个完整参数**保留下来。
同理 `"$@"` 也会保留每一个参数内部的空格，适合处理带空格的文件名。
